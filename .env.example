# SafeVault Backend 环境变量配置模板
# 复制此文件为 .env 并填入实际的值
# 警告：.env 文件包含敏感信息，切勿提交到 Git

# ========== 数据库配置 ==========
# 数据库连接 URL
DB_URL=jdbc:postgresql://localhost:5432/safevault

# 数据库用户名
DB_USER=postgres

# 数据库密码（至少12字符，包含大小写字母、数字、特殊字符）
DB_PASSWORD=your-strong-password-here

# ========== Redis 配置 ==========
# Redis 主机地址
REDIS_HOST=localhost

# Redis 端口
REDIS_PORT=6379

# Redis 密码（至少12字符）
REDIS_PASSWORD=your-redis-password-here

# ========== JWT 配置 ==========
# RSA 密钥对（Base64 编码的 PKCS8 格式）
# 生成命令（OpenSSL）：
#   openssl genrsa -out jwt_private.pem 2048
#   openssl rsa -in jwt_private.pem -pubout -out jwt_public.pem
#   openssl pkcs8 -topk8 -inform PEM -outform PEM -nocrypt -in jwt_private.pem -out jwt_private_pkcs8.pem
#   cat jwt_private_pkcs8.pem | base64 -w 0
#   cat jwt_public.pem | base64 -w 0
JWT_RSA_PRIVATE_KEY=your-rsa-private-key-base64-encoded-here
JWT_RSA_PUBLIC_KEY=your-rsa-public-key-base64-encoded-here

# ========== 邮件服务配置 ==========
# 邮件服务密码（阿里云邮件推送）
MAIL_PASSWORD=your-mail-password-here

# ========== HTTPS/SSL 配置 ==========
# Keystore 密码（用于 HTTPS）
KEYSTORE_PASSWORD=your-keystore-password-here

# ========== 密码哈希配置 ==========
# 密码哈希算法：argon2id（推荐）或 pbkdf2（向后兼容）
PASSWORD_HASH_ALGORITHM=argon2id

# Argon2id 参数配置
# 时间成本（迭代次数）：推荐 3-4，平衡安全性和性能
ARGON2_TIME_COST=3

# 内存成本（KB）：推荐 131072（128MB），可根据服务器内存调整
ARGON2_MEMORY_COST=131072

# 并行度（线程数）：推荐 2-4，建议不超过 CPU 核心数
ARGON2_PARALLELISM=4

# 输出长度（字节）：默认 32（256位）
ARGON2_OUTPUT_LENGTH=32

# 盐值长度（字节）：默认 16（128位）
ARGON2_SALT_LENGTH=16
